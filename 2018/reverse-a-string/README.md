# Objective
	Write a function that takes in a string and reverses its value.

# Example Returns
* myFunction("god") will return "dog".
* myFunction("dracula" will return "alucard".
* myFunction("javascript") will return "tpircsavaj".